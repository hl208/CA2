<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <title> Sneakers </title>
</head>
<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <div class="d-flex align-items-center">
        <a class="navbar-brand me-3" href="/shoes/">We Are Sneakers</a>
        <a href="/shoes/addSneakers" class="btn btn-outline-light btn-sm d-flex align-items-center gap-1 me-3">
          <i class="bi bi-plus-circle"></i>
          Add Sneakers
        </a> 
     </div>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="collapsibleNavbar">
        <ul class="navbar-nav align-items-center">
          <li class="nav-item me-3">
            <% if (session && session.user) { %>
              <span class="text-white">
                <i class="bi bi-person-circle fs-4"></i> <%= session.user.username %>
              </span>
            <% } %>
          </li>
          <li class="nav-item">
            <a class="btn btn-outline-light btn-sm mt-1" href="/logout">
              <i class="bi bi-box-arrow-right"></i> Logout
            </a>
          </li>
        </ul>
      </div>
    </div>
  </nav>
<!-- ChatGPT prompt (Hydhir): i want the line to be black colour, also make the border more curvys-->
    <div class="container my-5">
        <br>
        <div class="text-center mb-4"><h2 class="fw-bold text-uppercase text-primary">Sneakers info</h2></div>
        <br>
        <% if (shoe) { %> 
            <div class="p-4 bg-light rounded-4 shadow-sm fs-5 border border-3 border-dark">
                <p class="pb-3 mb-3 border-bottom"><b>Sneakers Brand:</b><br> <%= shoe.brand %></p>
                <p class="pb-3 mb-3 border-bottom"><b>Sneaker Model:</b><br>  <%= shoe.model %> ></p>
                <p class="pb-3 mb-3 border-bottom"><b>Description:</b><br> <%= shoe.description %></p>
                <p class="pb-3 mb-3 border-bottom"><b>Size:</b><br> <%= shoe.size %></p>
                <p class="pb-3 mb-3 border-bottom"><b>Condition:</b><br> <%= shoe.condition %></p>
                <p class="pb-3 mb-3 border-bottom"><b>Price:</b><br> <%= shoe.price %></p>
                <p class="pb-3 mb-3 border-bottom"><b>Created at:</b>:</b><br> <%= shoe.created_at %></p>
                <p class="pb-3 mb-3 border-bottom"><b>Sneaker Image:</b><br> <img src = "<%= shoe.image_path %>" class="img-fluid rounded mt-2 border " width="20%"></p>
                                

            </div>
        <% } else { %>
            <div class="alert alert-warning">No shoes found.</div>
        <% } %>
        <div class="mt-4">
        <a href ="/shoes/" class="btn btn-dark btn-lg px-4">Back</a>
        </div>

    <!-- ChatGPT: i want to add a favourite icon-->
        <form action="/shoes/favourites/add/<%= shoe.id %>" method="POST">
          <button type="submit" class="btn btn-danger btn-lg px-4">
          <i class="bi bi-heart"></i> Favourite
          </button>
        </form>
    </div>
    
</body>
</html>