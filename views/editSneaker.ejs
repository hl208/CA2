<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
  <title>Edit Sneaker</title>
</head>
<body>
  <div class="container py-5">
    <h1 class="mb-4">Edit Sneaker</h1>
    <form action="/shoes/editSneaker/<%= shoe.id %>" method="POST" enctype="multipart/form-data">
      <div class="mb-3">
        <label class="form-label">Brand</label>
        <input type="text" name="brand" class="form-control" value="<%= shoe.brand %>" required />
      </div>
      <div class="mb-3">
        <label class="form-label">Model</label>
        <input type="text" name="model" class="form-control" value="<%= shoe.model %>" required />
      </div>
      <div class="mb-3">
        <label class="form-label">Description</label>
        <textarea name="description" class="form-control" rows="3"><%= shoe.description %></textarea>
      </div>
      <div class="mb-3">
        <label class="form-label">Size</label>
        <input type="text" name="size" class="form-control" value="<%= shoe.size %>" required />
      </div>
      <div class="mb-3">
        <label class="form-label">Condition</label>
        <input type="text" name="condition" class="form-control" value="<%= shoe.condition %>" required />
      </div>
      <div class="mb-3">
        <label class="form-label">Price</label>
        <input type="number" step="0.01" name="price" class="form-control" value="<%= shoe.price %>" required />
      </div>
      <div class="mb-3">
        <label class="form-label">Current Image</label><br/>
        <% if (shoe.image_path) { %>
          <img src="<%= shoe.image_path %>" alt="Shoe Image" style="max-width: 200px;" />
        <% } else { %>
          <p>No image uploaded.</p>
        <% } %>
      </div>
      <div class="mb-3">
        <label class="form-label">Change Image (optional)</label>
        <input type="file" name="image_path" class="form-control" />
      </div>
      <button type="submit" class="btn btn-success">Update Sneaker</button>
      <a href="/shoes" class="btn btn-secondary ms-2">Cancel</a>
    </form>
  </div>
</body>
</html>
