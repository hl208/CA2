<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>My Favourite Shoes</title>
</head>
<body>
  <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/shoes/">We Are Sneakers</a>
    </div>
  </nav>

  <div class="container py-5">
    <h1 class="mb-4 text-center">My Favourite Shoes</h1>

    <% if (shoes.length === 0) { %>
      <div class="alert alert-info text-center">
        You have no favourite shoes yet
      </div>
    <% } else { %>
      <div class="row row-cols-1 row-cols-md-3 g-4">
        <% shoes.forEach(shoe => { %>
          <div class="col">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h5 class="card-title"><%= shoe.model %></h5>
                <p class="card-text">
                  Brand: <strong><%= shoe.brand %></strong><br>
                  Size: <%= shoe.size %><br>
                  Price: $<%= shoe.price %><br>
                  Condition: <%= shoe.condition %><br>
                  Posted by: <strong><%= shoe.username %></strong>
                </p>
                <% if (shoe.image_path) { %>
                  <img src="<%= shoe.image_path %>" class="card-img-bottom" style="max-height:200px; object-fit:cover;" alt="Shoe Image" />
                <% } %>
                <a href="/shoes/viewSneaker/<%= shoe.id %>" class="btn btn-primary mt-2">View Details</a>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } %>
  </div>
  <div class="mt-4 text-center">
    <a href="/shoes" class="btn btn-dark btn-lg px-4">
    <i class="bi bi-arrow-left"></i> Back Home
  </a>
</div>
</body>
</html>